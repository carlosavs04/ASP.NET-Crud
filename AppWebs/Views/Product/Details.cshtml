@model Products
@{
    ViewBag.Title = "Product Details";
}

<div class="wrapper">
    <div class="content-wrapper">
        <div class="content">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card mt-5">
                            <div class="card-header border-0">
                                <div class="card-header border-0 d-flex justify-content-between align-items-center">
                                    <div>
                                        <h2>@Html.DisplayFor(model => model.ProductName) </h2>
                                        <p class="lead mb-5">
                                            <strong>Description</strong>: @Html.DisplayFor(model => model.Description) <br>
                                            <strong>Standar Cost</strong>: @Html.DisplayFor(model => model.StandardCost) <br>
                                            <strong>List Price</strong>: @Html.DisplayFor(model => model.ListPrice) <br>
                                            <strong>Category</strong>: @(Model.Product_Categories != null ? Model.Product_Categories.CategoryName : "Empty")
                                        </p>
                                        <a asp-action="Index" class="btn btn-primary"><i class="bi bi-arrow-return-left"></i>&nbsp; Back to list</a>
                                    </div>
                                    @if (ViewBag.isEdit)
                                    {
                                        <a asp-action="Edit" asp-route-id="@Model.ProductId" class="btn btn-primary"><i class="bi bi-pencil-square"></i>&nbsp; Edit</a>
                                    }
                                    @if (ViewBag.isDelete)
                                    {
                                        <div class="text-center">
                                            <p>Are you sure you want to delete this record?</p>
                                            <a asp-action="DeleteProduct" asp-route-id="@Model.ProductId" class="btn btn-danger"><i class="bi bi-trash-fill"></i>&nbsp; Delete</a>
                                        </div>
                                    }
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>